using Quartz;

namespace SALLY_API.Walker.Jobs
{
    public class HandHygieneCleanUp : IJob
    {
        public async Task Execute(IJobExecutionContext context)
        {
            //APIService api = new APIService();
            //await api.FullCleanHandHygiene();
            //api.Dispose();
            //GlobalLogger.Logger.Debug("Clean Up Completed at: " + DateTime.Now);
            //await Task.CompletedTask;


        }
    }
}
